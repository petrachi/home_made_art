<h1>
  Sign in
</h1>

<%= simple_form_for :sign_in do |f| %>
  <%= f.input :email %>
  <%= f.input :password %>
  <%= f.input :remember_me, as: :boolean, inline_label: 'Yes, remember me' %>
  <%= f.button :submit %>
<% end %>
